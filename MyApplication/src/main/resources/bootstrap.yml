spring:
  application:
    name: MyApplication  # The application name (this must match the configuration in Git)
  cloud:
    config:
      uri: http://localhost:8888

server:
	port:8089

logging:
  level:
    org.springframework.cloud.config.client: DEBUG